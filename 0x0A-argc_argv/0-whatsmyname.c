#include <stdio.h>
#include "holberton.h"
/**
 * main - print its name + \n
 * @argc: number of argument type
 * @argv: array pointing to aruguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n", argv[argc]);
	return (0);
}
