#include <stdio.h>
#include <stdlib.h>
#include "3-calc.h"
/**
 * op_add - five functions
 * @a: input one
 * @b: input two
 * Return: always 0
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - five functions
 * @a: input one
 * @b: input two
 * Return: always 0
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - five functions
 * @a: input one
 * @b: input two
 * Return: always 0
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - five functions
 * @a: input one
 * @b: input two
 * Return: always 0
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - five functions
 * @a: input one
 * @b: input two
 * Return: always 0
 */
int op_mod(int a, int b)
{
	return (a % b);
}
