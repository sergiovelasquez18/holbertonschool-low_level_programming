#include <stdio.h>
/**
 * main - imprints alphabet
 *
 * Return: Always 0
*/
int main(void)
{
	char letra;

	for (letra = 'a'; letra <= 'z'; letra++)
	{
		putchar (letra);
	}
	putchar ('\n');
return (0);
}
